[features]
seeds = false
skip-lint = false

[programs.localnet]
mirror_sync = "YourProgramIDHere"

[programs.devnet]
mirror_sync = "YourProgramIDHere"

[programs.mainnet]
mirror_sync = "YourProgramIDHere"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
Add Anchor configuration
